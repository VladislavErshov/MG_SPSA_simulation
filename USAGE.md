# Usage

## Install

```bash
pip install -r requirements.txt
```

## Run simulation with SPSA vs GD comparison

```bash
python examples/comparison.py
```

Launches live visualization with both algorithms, then shows the final comparison dashboard. Outputs are saved to `results/`.

## Run tests

```bash
python -m pytest tests/
```
